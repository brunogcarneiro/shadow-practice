name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip
      - run: brew install portaudio
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e '.[dev]'
      - run: ruff check .
      - run: pytest
      - run: python -m build
      - run: python -m pip install --force-reinstall --no-deps dist/*.whl
      # Import outside the checkout so the compatibility launcher
      # `shadow_practice.py` cannot shadow the installed package.
      - run: cd "$RUNNER_TEMP" && python -c "import shadow_practice; import shadow_practice.cli"
